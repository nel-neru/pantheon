# Pantheon - Core Dependencies
# Python 3.11+
#
# Pantheon has NO hosted-LLM dependencies: all reasoning runs through the local
# `claude` CLI (Claude Code). There are no anthropic/openai/google API SDKs and
# no API keys.

# Core runtime
pydantic~=2.7
python-dotenv~=1.0
PyYAML~=6.0
PyGithub~=2.3
GitPython~=3.1
# langgraph is kept purely as a local orchestration library (no API calls).
langgraph~=0.2
langgraph-checkpoint-sqlite~=1.0

# Web GUI / API
fastapi~=0.111
httpx~=0.27
uvicorn[standard]~=0.30

# Testing / local development
pytest~=8.0
pytest-asyncio~=0.23
