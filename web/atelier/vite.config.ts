/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import path from 'node:path'

// Pantheon Atelier — a clean-room, art-forward GUI that runs PARALLEL to web/frontend.
// It builds to web/atelier/dist (its own folder) so it never clobbers web/dist, and
// proxies to the same FastAPI backend on :8000 in dev. Dev port is 5273 to avoid
// colliding with the legacy frontend's 5173.
export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5273,
    proxy: {
      '/api': 'http://localhost:8000',
      '/ws': { target: 'ws://localhost:8000', ws: true },
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    css: false,
  },
})
